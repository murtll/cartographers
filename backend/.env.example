# Скопировать в .env перед первым `docker compose up`.
# Это значения только для локальной разработки — нигде по-настоящему их не использовать.


DATABASE_URL=
COOKIE_SECRET_KEY=

ADDR=":8080"
HEALTHZ_ADDR=":9090"
LOG_LEVEL="INFO"
LOG_FORMAT="text"
